<!-- views/home.handlebars -->

<div class="container">
	<div class="row">
		<div class="col-lg">
			<h1> Welcome to Great Human Skills!</h1>
		</div>

		<div class="col-md-8">
			{{#if currentUser}}
			<p>Hi, {{currentUser.email}}! Check out what's new!</p>
			{{else}}
			{{/if}}
		</div>

	</div>
</div>